model:
  name: yolo
  display_name: YOLO11
  framework: ultralytics
  pretrained_checkpoint: yolo11n.pt

training:
  epochs: 50
  batch_size: 16
  image_size: 640
  workers: 4
  device: 0
  optimizer: auto
  learning_rate: null
  weight_decay: null
  fraction: 1.0
  max_minutes: null
  patience: 10
  seed: 42
  deterministic: true

augmentation:
  horizontal_flip: true
  color_jitter: true
  random_crop: false

results:
  experiment_prefix: yolo
